export default class Experience {
    constructor(type, title, description, startDate, endDate) {
        this.type = type;
        this.startDate = startDate;
        this.endDate = endDate;
        this.title = title;
        this.description = description;
    }
}