export default class Education {
    constructor(startDate, endDate, school, degree, description) {
        this.startDate = startDate;
        this.endDate = endDate;
        this.school = school;
        this.degree = degree;
        this.description = description;
    }
}